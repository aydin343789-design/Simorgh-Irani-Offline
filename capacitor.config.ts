import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.simorgh.irani',
  appName: 'SIMURGH',
  webDir: 'www',
  android: {
    backgroundColor: '#0b1020'
  }
};

export default config;
